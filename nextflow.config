manifest.mainScript = 'main.nf'

process {
    container = 'goldrieve/vsgseq2:latest'
    conda.enabled = true
}

docker {
    enabled = 'true'
    fixOwnership = 'true'
    runOptions = '--user "$(id -u):$(id -g)"'
}

singularity {
    enabled = false
    autoMount = true 
}