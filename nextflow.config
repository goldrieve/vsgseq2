manifest.mainScript = 'main.nf'
conda.enabled = true

process {
    container = 'goldrieve/vsgseq2:latest'

    conda.enabled = true
}

docker {
    enabled = true // Enable Docker if available
    runOptions = '-u $(id -u):$(id -g)' // Ensure proper file permissions
}