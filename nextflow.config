manifest.mainScript = 'main.nf'

process {
    conda.enabled = true
}

docker {
    enabled = 'false'
    fixOwnership = 'true'
    runOptions = '--user "$(id -u):$(id -g)"'
}

singularity {
    enabled = false
    autoMount = true 
}
